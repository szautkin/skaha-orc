apiVersion: v1
kind: Service
metadata:
  name: {{ include "reg.fullname" . }}
  labels:
    {{- include "reg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reg.selectorLabels" . | nindent 4 }}
